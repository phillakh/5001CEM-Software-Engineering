<!doctype html>

<html lang="en">
    <head>
        <title> The Gallery</title>
        <link href="gallery.css" type="text/css" rel="stylesheet"/>
        <meta charset="utf-8"/>
    </head>
    <body>
        <h1> The Gallery</h1>
        <nav class = "nav"> 
            <a class="current" href="/">Home</a>
            <a href="#login">Login</a>
            <a href="#about">About Us</a>
            <a href="#support">Support</a>
            <input type="text" placeholder="Search ...">
        </nav> 
                
        
        {{#each}}
        <section class = "row">
                {{#each}}
                <section class = "column">
                    <figure>
                        <a href="art"></a>
                        <img src="images/cafe.jpg" alt="pale blue electric guitar" width = "100" height = "100"/></a>
                        <figcaption> {{body.author}} {{body.title}},{{body.price}} {{body.location}}</figcaption>
                    </figure>
                </section>
                {{/each}}
        </section>
        {{/each}}
    </body>
</html>